# =============================================================================
# Piper TTS Server Dependencies
# =============================================================================
# These are the Python packages required to run the TTS server.
# Install with: pip install -r requirements.txt
# =============================================================================

# FastAPI - Modern web framework for building APIs
# Used to create HTTP endpoints for TTS
fastapi==0.104.1

# Uvicorn - ASGI server to run FastAPI
# Used to serve the HTTP API
uvicorn==0.24.0

# Pydantic - Data validation (comes with FastAPI, but pinning version)
# Used for request/response models
pydantic==2.5.0

# Python-multipart - For file uploads (if needed in future)
python-multipart==0.0.6

