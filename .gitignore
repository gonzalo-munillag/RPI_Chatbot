# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/
train_venv/
wakeword/train_venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
*.log

# Ollama models (if testing locally)
.ollama/

# Piper TTS voice models (large files, downloaded during Docker build)
piper-tts/voices/*.onnx
piper-tts/voices/*.onnx.json

# Whisper STT models (large files, downloaded on first run)
whisper-stt/models/*
!whisper-stt/models/.gitkeep

# Environment variables (contains sensitive data)
.env

# blog post files
BLOG_POST.md
BLOG_WEBPORTAL.md
BLOG_VOICE.md
BLOG_ALTERNATIVES.md

# quick ssh into the pi
ssh_pi.sh
send_env2pi.sh

# Wake word training samples (for custom wake word training via Colab)
wakeword/training_samples/

# Trained wake word models
wakeword/models/*.onnx
!wakeword/models/.gitkeep

# Training virtual environments
wakeword/train_venv/